import { FC, useEffect, useState } from 'react';

const Logout: FC = () => {
  // const [isReady, setIsReady] = useState(false)

  // useEffect(() => {
  //   ;(async () => {
  //   })();
  // }, []);

  return <div>
    logout
  </div>;
};

export default Logout;